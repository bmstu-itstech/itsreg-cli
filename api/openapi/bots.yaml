openapi: 3.0.3
info:
  title: ITS Reg API
  description: ITS Reg API
  version: 2.0.0
servers:
  - url: "{url}"
security:
  - bearerAuth: []

paths:
  /bots:
    put:
      operationId: createBot
      description: Создать или заменить существующего бота с данным ID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PutBots'
      responses:
        "201":
          description: Бот успешно создан.
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

    get:
      operationId: getBots
      description: Получить информацию о ботах пользователя
      responses:
        "200":
          description: Успешно получена информация о ботах.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Bot'
        "400":
          description: "Данные в запросе невалидны."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}:
    get:
      operationId: getBot
      description: Получить информацию о боте с указанным ID.
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "200":
          description: Бот найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Bot'
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

    delete:
      operationId: deleteBot
      description: Удалить бота с указанным ID
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "204":
          description: Бот успешно удалён.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Bot'
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}/status:
    get:
      operationId: getStatus
      description: Получить статус инстанса бота в данный момент времени.
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "200":
          description: Успешно получен статус бота
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Status'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}/answers:
    get:
      operationId: getAnswers
      description: Получить ответы участников на бота с данным ID в формате CSV.
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "200":
          description: Успешно получены ответы участников.
          content:
            text/csv: { }
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}/start:
    post:
      operationId: startBot
      description: Запустить бота с указанным ID
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "204":
          description: Бот успешно запущен.
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}/stop:
    post:
      operationId: stopBot
      description: Остановить бота с указанным ID
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "204":
          description: Бот успешно остановлен.
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}/enable:
    post:
      operationId: enableBot
      description: Включить автозапуск для бота
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "204":
          description: Бот успешно запущен и добавлен в автозапуск.
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}/disable:
    post:
      operationId: disableBot
      description: Выключить автозапуск бота
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      responses:
        "204":
          description: Бот удалён из автозапуска.
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

  /bots/{id}/mailing:
    post:
      operationId: mailing
      description: Сделать рассылку по массиву пользователей.
      parameters:
        - in: path
          name: id
          schema:
            type: string
            example: example_bot
          required: true
          description: Уникальный ID бота.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PostMailing'
      responses:
        "200":
          description: Рассылка прошла успешно.
        "400":
          description: Данные в запросе невалидны.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "401":
          description: Не был указан JWT токен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'
        "404":
          description: Бот с данным ID не найден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlainError'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Predicate:
      type: object
      description: Predicate описывает условие перехода по ребру.
      oneOf:
        - $ref: '#/components/schemas/AlwaysPredicate'
        - $ref: '#/components/schemas/ExactPredicate'
        - $ref: '#/components/schemas/RegexPredicate'
      discriminator:
        propertyName: type
        mapping:
          always: '#/components/schemas/AlwaysPredicate'
          exact:  '#/components/schemas/ExactPredicate'
          regex:  '#/components/schemas/RegexPredicate'

    AlwaysPredicate:
      type: object
      description: Переход по ребру осуществляется на любое сообщение пользователя.
      properties:
        type:
          type: string
          enum: [always]
      required:
        - type

    ExactPredicate:
      type: object
      description: Переход по ребру осуществляется при полном совпадении строки text с сообщением пользователя.
      properties:
        type:
          type: string
          enum: [exact]
        text:
          type: string
      required:
        - type
        - text

    RegexPredicate:
      type: object
      description: Переход по ребру осуществляется при совпадении с регулярным выражением pattern.
      properties:
        type:
          type: string
          enum: [regex]
        pattern:
          type: string
      required:
        - type
        - pattern

    Edge:
      type: object
      description: Обозначают связь между узлами как переход в результате ответа пользователя.
      properties:
        predicate:
          $ref: '#/components/schemas/Predicate'
        to:
          type: integer
          description: State узла, к которому совершается переход.
        operation:
          type: string
          description: >
            Действие, которое выполнится в результате перехода пользователя по ребру.
            - noop. Ничего не происходит. Подходит для использования в меню и промежуточных узлах.
            - save. Сохраняет ответ или перезаписывает предыдущий. Подходит в большинстве ситуаций.
            - append. Добавляет ответ к предыдущему. Подходит для вопросов с множественным выбором.
          enum:
            - noop
            - save
            - append
      required:
        - predicate
        - to
        - operation

    Message:
      type: object
      description: Любое сообщение в Telegram. На данный момент описывается текстом, но в будущем добавится поддержка файлов.
      properties:
        text:
          type: string
      required:
        - text

    Node:
      type: object
      description: >
        Минимальная структурная единица сценария бота. Представляет собой сообщение (сообщения), которые отправляются
        пользователю. Ожидается ответ пользователя для перехода к следующему узлу.
      properties:
        state:
          type: integer
          description: Уникальный номер узла в сценарии бота.
        title:
          type: string
          description: Человеко-читаемое название узла. В таблице ответов будет отображаться как заголовок столбца.
        edges:
          type: array
          description: Массив исходящих рёбер узла.
          items:
            $ref: '#/components/schemas/Edge'
        messages:
          type: array
          description: Массив отправляемых ботом сообщений при вхождении в узел.
          items:
            $ref: '#/components/schemas/Message'
        options:
          type: array
          description: Массив кнопок (опций) ответа для пользователя.
          items:
            type: string
      required:
        - state
        - title
        - messages

    Entry:
      type: object
      description: >
        Точка входа в сценарий бота. Пользователь может вызвать точку входу командой /<entry> (как, например, /start).
        Может быть вызвана рассылкой по такому же ключу.
      properties:
        key:
          type: string
          description: Ключ точки входа.
        start:
          type: integer
          description: Указатель на State узла, с которого начинается выполнение сценария.
      required:
        - key
        - start

    Script:
      type: object
      description: Сценарий бота.
      properties:
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/Node'
        entries:
          type: array
          items:
            $ref: '#/components/schemas/Entry'
      required:
        - nodes
        - entries

    Bot:
      type: object
      properties:
        id:
          type: string
          description: Уникальный ID бота.
        token:
          type: string
          description: Телеграм токен для бота, полученный в @BotFather.
        author:
          type: integer
          format: int64
          description: ID пользователя - автора бота.
        enabled:
          type: boolean
          description: Автозапуск бота
        script:
          $ref: '#/components/schemas/Script'
      required:
        - id
        - token
        - author
        - enabled
        - script

    PutBots:
      type: object
      properties:
        id:
          type: string
          description: Уникальный ID бота.
        token:
          type: string
          description: Телеграм токен для бота, полученный в @BotFather.
        script:
          $ref: '#/components/schemas/Script'
      required:
        - id
        - token
        - script

    PlainError:
      type: object
      properties:
        message:
          type: string
          example: error message
      required:
        - message

    Status:
      type: string
      enum:
        - running
        - idle
        - dead
      description: Статус инстанса бота.

    PostMailing:
      type: object
      properties:
        entryKey:
          type: string
          description: Ключ точки входа, которая будет выполнена для списка пользователей.
        users:
          type: array
          items:
            type: integer
            format: int64
          description: Список пользователей, для которых будет выполнен скрипт начиная с точки входа entryKey.
      required:
        - entryKey
        - users

    InvalidInputError:
      type: object
      properties:
        code:
          type: string
          example: message-empty-text
        message:
          type: string
          example: expected not empty message text
        details:
          type: object
          additionalProperties:
            type: string
          example:
            field: text
            state: 5
      required:
        - code
        - message

    Error:
      oneOf:
        - $ref: '#/components/schemas/PlainError'
        - $ref: '#/components/schemas/InvalidInputError'
        - type: array
          items:
            oneOf:
              - $ref: '#/components/schemas/PlainError'
              - $ref: '#/components/schemas/InvalidInputError'
      example:
        - code: message-empty-text
          message: expected not empty message text
          details:
            field: text
            state: 5
        - code: edge-invalid-pattern
          message: failed to compile regex pattern
          details:
            field: pattern
            state: 4
            pattern: some-invalid-pattern
